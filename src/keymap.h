#pragma once

#include "raylib.h"

const KeyboardKey SELECTED_BLOCK_LEFT = KEY_A;
const KeyboardKey SELECTED_BLOCK_RIGHT = KEY_D;

const KeyboardKey INCREASE_PLACE_WIDTH = KEY_EQUAL;
const KeyboardKey DECREASE_PLACE_WIDTH = KEY_MINUS;
